<!-- Navigation -->
<nav class="navbar navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="container-fluid topnav">

     <!-- Brand and toggle get grouped for better mobile display -->
     <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to image_tag("spotted_logo.jpg", class: 'img-responsive', alt: "Spotted Logo"), root_path, class: 'navbar-brand' %>
    </div>
    <!-- /.navbar-header -->

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Hi, #{current_user.profile.first_name}", profile_path(current_user.profile.id) %></li>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign In", new_user_session_path %></li>
        <% end %>
        <li>
          <%= link_to "Home", root_path %>
        </li>
        <li>
          <%= link_to "FAQ", faq_path %>
        </li>
      </ul>
    </div>
  </div>
  <!-- /.navbar-collapse -->
</div>
<!-- /.container -->
</nav>
<!-- End of Navigation Section -->

<p id="notice"><%= notice %></p>

<div class="container-fluid top-row col-xs-12 col-sm-12 col-lg-12 col-xl-12">

<h1>All Advertisements</h1>

<div class="table-responsive">
  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th>User First Name</th>
        <th>Business Name</th>
        <th>Business Number</th>
        <th>Advert Title</th>
        <th>Advert Image</th>
        <th>Advert Description</th>
        <th>Advert Period</th>
        <th>Advert Location</th>
        <th>Vehicle Sticker Location</th>
        <th>Payout per Mile (AUD &#36;)</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @adverts.each do |advert| %>
        <tr>
          <td><%= advert.user.profile.first_name %></td>
          <td><%= advert.business_name %></td>
          <td><%= advert.business_number %></td>
          <td><%= advert.advert_title %></td>
          <td><%= image_tag advert.advert_image.url unless advert.advert_image.blank? %></td>
          <td><%= advert.advert_description %></td>
          <td><%= advert.advert_period %></td>
          <td><%= advert.advert_location %></td>
          <td><%= advert.advert_sticker_location %></td>
          <td><%= advert.payout_per_mile %></td>
          <td><%= link_to 'Show', advert, class: 'btn id-advert-link' %></td>
          <td><%= link_to 'Edit', edit_advert_path(advert), class: 'btn edit-advert-link' %></td>
          <td><%= link_to 'Destroy', advert, class: 'btn destroy-advert-link', method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>

<div class="container-fluid  col-xs-12 col-sm-12 col-lg-12 col-xl-12">
  <%= link_to 'New Advertisement', new_advert_path, class: 'btn new-advert-link' %>
  <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'btn sign-out-link' %>
</div>
